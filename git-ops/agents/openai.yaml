interface:
  display_name: "Git Ops"
  short_description: "Git workflow for task-based worktree, commit, push, PR creation, and CI."
  default_prompt: "Use $git-ops when asked to commit, push, create a PR, or separate tasks with worktrees."
