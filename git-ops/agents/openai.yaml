interface:
  display_name: "Git Ops"
  short_description: "Git workflow for mandatory parallelization assessment, worktree/thread separation, commit, push, PR creation, and CI."
  default_prompt: "Use $git-ops when asked to commit, push, create a PR, and always assess parallelization; if possible, recommend a dedicated thread/worktree."
