interface:
  display_name: "App Store Preflight Compliance"
  short_description: "Apple App Store preflight compliance workflow"
  default_prompt: "Use $app-store-preflight-compliance to run Greenlight preflight with a pinned release, prefer API-key auth, fix compliance issues, and rerun until GREENLIT."
